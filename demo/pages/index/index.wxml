<view class="container">
  <filterbar twoStageDataLeft="{{ twoStageDataLeft }}" radioData="{{ radioData }}" sortData="{{ sortData }}" filterData="{{ filterData }}" top="310rpx" bind:filter="onFilter" />
  <view class="list">
    <view class="item">
      ABC
    </view>
    <view class="item">
      CDE
    </view>
  </view>
</view>